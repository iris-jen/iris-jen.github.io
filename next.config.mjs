/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: "export",
    reactStrictMode: true,
    images: {
        unoptimized: false, // Disable Image Optimization
    },
};

export default nextConfig;
